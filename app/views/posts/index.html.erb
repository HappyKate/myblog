<h2>Главная</h2>
<% @posts.each do |post| %>
  <div class="well">
    <h4 class="title"><%=link_to post.title, post %></h4>
    <p class="date"><%= post.created_at.strftime("%d, %B, %Y")%></p>
    <p><%= post.body%></p>
    <hr>
  </div>
<% end %>